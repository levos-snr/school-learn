/**
 * A React component that renders nothing.
 *
 * This parameterless component returns `null`, producing no DOM output when rendered.
 *
 * @returns `null` — nothing is rendered.
 */
export default function Loading() {
  return null
}
