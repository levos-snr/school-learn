"use client"

import { AdminLayout } from "@/components/admin/admin-layout"

/**
 * Root client component for the admin page.
 *
 * Renders the admin layout wrapper used for all admin routes.
 *
 * @returns The admin page layout as a React element.
 */
export default function AdminPage() {
  return <AdminLayout />
}
