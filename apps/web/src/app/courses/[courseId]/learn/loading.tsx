/**
 * React component that intentionally renders nothing.
 *
 * This component returns `null` and acts as a no-op placeholder (e.g., for route-level loading/suspense).
 */
export default function Loading() {
  return null
}
